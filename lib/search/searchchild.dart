import 'package:flutter/material.dart';

class cariAnak extends StatelessWidget {
  const cariAnak({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    child: Row(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [Text('Cari anak ?')],
    ),
    );
  }
}
